# calendar-feed Edge Function

Deploy:

```bash
supabase functions deploy calendar-feed --no-verify-jwt
```

URL:

```text
https://DIN_PROJECT_REF.supabase.co/functions/v1/calendar-feed
```

Per montör:

```text
https://DIN_PROJECT_REF.supabase.co/functions/v1/calendar-feed?installer=MONTOR_UUID
```
